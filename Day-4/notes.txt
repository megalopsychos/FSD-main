Event Delegation

Event Delegation is a technique in JavaScript where you attach a single event listener to a parent element instead of multiple event listeners to each child element. This is useful when you have a lot of elements that require similar behavior (like buttons or list items).